server:
  port: 8080
spring:
  application:
    name: customer
  datasource:
    password: jedrom
    url: jdbc:postgresql://localhost:5432/customer
    username: jedrom
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true